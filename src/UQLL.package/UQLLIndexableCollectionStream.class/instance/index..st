double-dispatch
index: aCollection
	" This is also a double dispatch + decorator. This is the only stream that needs an index. The rest of the streams only delegate into the internal source  "
	index := aCollection 