double-dispatch
index: anIndex
"This is  a double dispatch + decorator. the only stream that needs an index is the indexable collection stream . The rest of the streams only delegate into the internal source  "
	source index: anIndex